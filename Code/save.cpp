#include <ftream>
#include "partie.h"
#include <string>
/*
bool Save::savePartie(const string& nameFich) {
  string nomFich= "partie"+ to_string(Partie::getID())+".txt";
  ofstream fichier(nomFich);
  if (!fichier.is_open()) {
        cout << " Erreur : impossible de créer le fichier de sauvegarde" << endl;
        return false;
    }
    
    */
